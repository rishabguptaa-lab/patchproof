import { createHmac, createSign, timingSafeEqual } from 'node:crypto';
export function verifyWebhook(secret,body,signature=''){const expected=`sha256=${createHmac('sha256',secret).update(body).digest('hex')}`;const a=Buffer.from(expected),b=Buffer.from(signature);return a.length===b.length&&timingSafeEqual(a,b);}
export function appJwt(appId,privateKey,now=Math.floor(Date.now()/1000)){const header=b64({alg:'RS256',typ:'JWT'}),payload=b64({iat:now-60,exp:now+540,iss:String(appId)});const input=`${header}.${payload}`;const signer=createSign('RSA-SHA256');signer.update(input);return`${input}.${signer.sign(privateKey).toString('base64url')}`;}
export async function installationToken(api,appId,key,installationId){const response=await fetch(`${api}/app/installations/${installationId}/access_tokens`,{method:'POST',headers:{authorization:`Bearer ${appJwt(appId,key)}`,accept:'application/vnd.github+json','x-github-api-version':'2022-11-28'}});if(!response.ok)throw new Error(`Installation token failed: ${response.status}`);return(await response.json()).token;}
export async function gh(api,token,endpoint,options={}){const response=await fetch(`${api}${endpoint}`,{...options,headers:{authorization:`Bearer ${token}`,accept:'application/vnd.github+json','content-type':'application/json','x-github-api-version':'2022-11-28',...(options.headers||{})}});if(!response.ok)throw new Error(`GitHub API ${endpoint} failed: ${response.status} ${await response.text()}`);return response.status===204?null:response.json();}
function b64(value){return Buffer.from(JSON.stringify(value)).toString('base64url');}
